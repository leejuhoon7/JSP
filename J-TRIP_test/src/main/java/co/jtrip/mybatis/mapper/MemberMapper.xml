<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="co.jtrip.test.mapper.MemberMapper">
	

	<select id="memberSelect" resultType="co.jtrip.member.service.MemberVO">
		SELECT * FROM MEMBER
		WHERE MEMBER_ID = #{memberId}
		<if test="memberPassword !=null">
			AND MEMBER_PASSWORD = #{memberPassword}
		</if>
	</select>

	<update id="memberUpdate" parameterType="co.jtrip.member.service.MemberVO">
		UPDATE MEMBER
		SET
		MEMBER_PASSWORD = #{memberPassword},
		MEMBER_ADDREESS = #{memberAddress},
		MEMBER_EMAIL=#{memberEmail},
		MEMBER_TEL = #{memberTel},
		WHERE
		MEMBER_ID = #{memberId}
	</update>
	
	<!-- insert -->


</mapper>